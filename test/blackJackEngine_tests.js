const expect 
const blackjack = require('../public/javascript/blackJackEngine')

describe('blackjackEngine', () => {
    it('describe test', () => {
        // finish test cases
        const result = blackjack.functionname()
        expect(result).to.equal(some value)
    })
})